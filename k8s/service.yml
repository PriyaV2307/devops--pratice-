apiVersion: v1
kind: Service

metadata:
  name: devops-practice-service

spec:
  type: NodePort

  selector:
    app: devops-practice

  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30007